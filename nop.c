#include "monty.h"

/**
* f_nop - does nothing
* @head: stack head
* @counter: line number
* @bus: bus structure
* Return: no return
*/
void f_nop(stack_t **head, unsigned int counter, bus_t *bus)
{
(void) head;
(void) counter;
(void) bus;
}
